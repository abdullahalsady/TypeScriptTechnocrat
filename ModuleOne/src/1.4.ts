{

// string
let firstName: string = "Sady";

// number
let roll: number = 10;

// boolean
let isAdmin: boolean = true;

// undefined
let x: undefined = undefined;


// null
let y: null = null;

// any (a special type in TypeScript; not recommended, as using the any type is like going back to plain JavaScript)
let z;

// Non Premetive datatype

// array
let friends: string[] = ["Sakib", "Sabit"]
friends.push("Shoven")
let ageList: number[] = [12, 32]

// tuple



let coordinates: [number, number] = [10, 20]
let ageName: [string, number, boolean] = ['Opurbo', 10, true]
ageName[0] = "Bangladesh";


}