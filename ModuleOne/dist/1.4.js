"use strict";
{
    // string
    let firstName = "Sady";
    // number
    let roll = 10;
    // boolean
    let isAdmin = true;
    // undefined
    let x = undefined;
    // null
    let y = null;
    // any (a special type in TypeScript; not recommended, as using the any type is like going back to plain JavaScript)
    let z;
    // Non Premetive datatype
    // array
    let friends = ["Sakib", "Sabit"];
    friends.push("Shoven");
    let ageList = [12, 32];
    // tuple
    let coordinates = [10, 20];
    let ageName = ['Opurbo', 10, true];
    ageName[0] = "Bangladesh";
}
